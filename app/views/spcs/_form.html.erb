<%= form_for(@spc) do |f| %>
  <% if @spc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spc.errors.count, "error") %> prohibited this spc from being saved:</h2>

      <ul>
      <% @spc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.text_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
