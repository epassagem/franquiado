<%= form_for(@cpf) do |f| %>
  <% if @cpf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cpf.errors.count, "error") %> prohibited this cpf from being saved:</h2>

      <ul>
      <% @cpf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.text_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
