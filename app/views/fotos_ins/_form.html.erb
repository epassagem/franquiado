<%= form_for(@fotos_in) do |f| %>
  <% if @fotos_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fotos_in.errors.count, "error") %> prohibited this fotos_in from being saved:</h2>

      <ul>
      <% @fotos_in.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inauguracao_id %><br>
    <%= f.number_field :inauguracao_id %>
  </div>
  <div class="field">
    <%= f.label :foto %><br>
    <%= f.text_field :foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
