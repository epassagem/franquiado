<%= form_for(@socio_irpj) do |f| %>
  <% if @socio_irpj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@socio_irpj.errors.count, "error") %> prohibited this socio_irpj from being saved:</h2>

      <ul>
      <% @socio_irpj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.text_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
