<%= form_for(@fotos_proj) do |f| %>
  <% if @fotos_proj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fotos_proj.errors.count, "error") %> prohibited this fotos_proj from being saved:</h2>

      <ul>
      <% @fotos_proj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aprov_proj_id %><br>
    <%= f.number_field :aprov_proj_id %>
  </div>
  <div class="field">
    <%= f.label :foto %><br>
    <%= f.text_field :foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
