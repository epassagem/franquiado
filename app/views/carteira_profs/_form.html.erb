<%= form_for(@carteira_prof) do |f| %>
  <% if @carteira_prof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carteira_prof.errors.count, "error") %> prohibited this carteira_prof from being saved:</h2>

      <ul>
      <% @carteira_prof.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.text_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
