<%= form_for(@proj_arquitetonico) do |f| %>
  <% if @proj_arquitetonico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proj_arquitetonico.errors.count, "error") %> prohibited this proj_arquitetonico from being saved:</h2>

      <ul>
      <% @proj_arquitetonico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.text_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
