<%= form_for(@cof) do |f| %>
  <% if @cof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cof.errors.count, "error") %> prohibited this cof from being saved:</h2>

      <ul>
      <% @cof.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interessado_id %><br>
    <%= f.number_field :interessado_id %>
  </div>
  <div class="field">
    <%= f.label :data_fim %><br>
    <%= f.date_select :data_fim %>
  </div>
  <div class="field">
    <%= f.label :estatus %><br>
    <%= f.check_box :estatus %>
  </div>
  <div class="field">
    <%= f.label :arquivo %><br>
    <%= f.file_field :arquivo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
